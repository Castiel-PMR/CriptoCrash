# 📊 CryptoChart - Компонент графика криптовалют

Автономный компонент для отображения графика свечей криптовалют с данными из Binance API.

## 🎯 Возможности

✅ **Японские свечи** - классическое отображение OHLC данных  
✅ **Умное форматирование цены** - автоматическая точность от 0 до 12 знаков  
✅ **Шкала цен** - 8 уровней с метками справа  
✅ **Live индикатор** - пульсирующая точка при свежих данных  
✅ **Оптимизация** - автоматическое прореживание при большом количестве свечей  
✅ **Fallback API** - Futures → Spot автоматически  
✅ **Настраиваемая прозрачность** - от 0 до 100%  

## 🚀 Использование

### Базовый пример

```tsx
import CryptoChart from './CryptoChart';

function App() {
  return (
    <div style={{ width: '800px', height: '400px' }}>
      <CryptoChart />
    </div>
  );
}
```

### С параметрами

```tsx
<CryptoChart
  symbol="ETHUSDT"          // Символ монеты (по умолчанию BTCUSDT)
  timeframe="1h"            // Таймфрейм: 1m, 5m, 15m, 30m, 1h, 4h, 1d
  opacity={80}              // Прозрачность 0-100 (по умолчанию 100)
  updateInterval={15000}    // Обновление каждые 15 сек (по умолчанию 10000)
  candleLimit={100}         // Количество свечей (по умолчанию 60)
/>
```

### Примеры для разных монет

```tsx
// Bitcoin 15-минутный график
<CryptoChart symbol="BTCUSDT" timeframe="15m" />

// Ethereum 1-часовой график
<CryptoChart symbol="ETHUSDT" timeframe="1h" />

// Solana 5-минутный график с полупрозрачностью
<CryptoChart symbol="SOLUSDT" timeframe="5m" opacity={60} />

// DOGE 1-дневный график с большим количеством свечей
<CryptoChart symbol="DOGEUSDT" timeframe="1d" candleLimit={120} />
```

## 📝 Пропсы

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `symbol` | `string` | `'BTCUSDT'` | Символ торговой пары (должен существовать на Binance) |
| `timeframe` | `string` | `'15m'` | Таймфрейм: `1m`, `5m`, `15m`, `30m`, `1h`, `4h`, `1d` |
| `opacity` | `number` | `100` | Прозрачность графика (0-100) |
| `updateInterval` | `number` | `10000` | Интервал обновления данных в миллисекундах |
| `candleLimit` | `number` | `60` | Количество отображаемых свечей |

## 🎨 Стилизация

Компонент использует canvas и занимает 100% ширины и высоты родителя.

```tsx
// Контейнер должен иметь размеры
<div style={{ width: '1000px', height: '600px' }}>
  <CryptoChart />
</div>

// Или с Tailwind CSS
<div className="w-full h-96">
  <CryptoChart />
</div>
```

## 🔧 Технические детали

### Цветовая схема (монохром)
- **Зеленые свечи (рост)**: контур `#888888`, заливка `#0a0a0a`
- **Красные свечи (падение)**: контур и заливка `#333333`
- **Текущая цена**: красная линия `#ff6666`
- **Live индикатор**: зеленая пульсирующая точка `#00ff88`

### Форматирование цен
Автоматическое определение точности:
- `≥ 10000` → 0 знаков (BTC, ETH)
- `1000-10000` → 1 знак
- `100-1000` → 2 знака
- `10-100` → 3 знака
- `1-10` → 4 знака
- `0.1-1` → 5 знаков
- `0.01-0.1` → 6 знаков
- ... и так далее до 12 знаков для SHIB/PEPE

### API Endpoints
1. **Futures API** (приоритет): `https://fapi.binance.com/fapi/v1/klines`
2. **Spot API** (fallback): `https://api.binance.com/api/v3/klines`

### Оптимизации
- При >100 свечах отрисовывается каждая вторая
- Автоматическая фильтрация пустых/невалидных свечей
- Адаптивная ширина свечей в зависимости от количества

## 📦 Зависимости

- React 18+
- TypeScript (опционально, но рекомендуется)

Никаких внешних библиотек - чистый Canvas API!

## 🐛 Troubleshooting

### График не отображается
- Проверьте, что контейнер имеет размеры (width и height)
- Проверьте консоль на ошибки API
- Убедитесь, что символ существует на Binance (например, `BTCUSDT`, `ETHUSDT`)

### Слишком медленное обновление
- Уменьшите `updateInterval` (например, `5000` для 5 секунд)
- Уменьшите `candleLimit` для меньшего количества данных

### Проблемы с CORS
Binance API поддерживает CORS, но если есть проблемы:
- Используйте прокси-сервер
- Или настройте серверную часть для запросов

## 📄 Лицензия

Свободное использование. Взято из проекта CriptoCrash.

## 🙏 Credits

Основано на коде визуализации ликвидаций CriptoCrash  
Автор: Castiel-PMR  
Репозиторий: https://github.com/Castiel-PMR/CriptoCrash
